# Copy–paste commands if you prefer the terminal over the UI clicks.
# Run these in VS Code Terminal (Ctrl + `) after opening this folder.

# Initialize and make the first commit
git init
git add index.js
git commit -m "Initial commit"

# Connect to your (empty) GitHub repository – replace USERNAME/REPO
git remote add origin https://github.com/USERNAME/my-first-repo.git
git branch -M main
git push -u origin main

# Create and switch to a feature branch
git checkout -b feature-login

# (Make a small change to index.js, then commit)
git add index.js
git commit -m "Update index.js on feature-login"
git push -u origin feature-login

# Switch back to main when done
git checkout main
